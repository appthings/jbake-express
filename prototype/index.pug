doctype html
html
	head
		meta(charset='utf-8')
		meta(name='viewport', content='width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no, minimal-ui, viewport-fit=cover')

		title jB X
		link(rel='stylesheet', href='//cdn.jsdelivr.net/npm/framework7@2.0.7/dist/css/framework7.css')
		link(rel='stylesheet', href='//cdn.jsdelivr.net/npm/framework7-icons@0.8.9/css/framework7-icons.css')
		link(rel='stylesheet', href='//cdn.jsdelivr.net/npm/material-icons-css@1.0.1/css/material-icons.css')

	body

		#app
				//.page
				//right
				.panel.panel-left.panel-cover
						.view
							.page(data-name='panel-1') 
								.navbar
									.navbar-inner
										.title jB *
								.page-content
									.block Menu

				// fixed main view .page-content 
				.view
					.page
						//fixed header
						.navbar
							.navbar-inner
								.left
								.title.sliding jB *

						// fixed footer		
						.toolbar.toolbar-bottom-md
							.toolbar-inner
								.block jB *

						.page-content
							.view.view-main.ios-edges
									//this gets replaced on href link:
									.page(data-name='home') 
										.block
											p oh hi
											a(href='/about/') About


		script(type='text/javascript', src='//cdn.jsdelivr.net/npm/framework7@2.0.6/dist/js/framework7.js')

		script.
			var routes_ =  [
				{ path: '/about/', url: 'about.html'}
			]

			var app = new Framework7({
				root: '#app',
				routes: routes_,
				on: {
					init: function () {
						console.log('app')
					}
				}
			})
			
			var mainView = app.views.create('.view-main')// ??

		style.
